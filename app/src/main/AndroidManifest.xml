<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.donutsbite.godofmem">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GodOfMem">
        <activity
            android:name="com.donutsbite.godofmem.feature.main.MainActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".feature.login.LoginActivity" />
        <activity android:name=".feature.book.BookListActivity" />
        <activity android:name=".feature.chapter.ChapterListActivity" />
        <activity android:name=".feature.quiz.QuizActivity" />
        <activity android:name=".feature.question.QuestionListActivity" />
    </application>

</manifest>